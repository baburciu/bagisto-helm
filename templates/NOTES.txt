Thank you for installing {{ .Chart.Name }}!

Your Bagisto e-commerce platform has been deployed.

1. Get the application service:
   kubectl get svc {{ include "bagisto.fullname" . }} -n {{ .Release.Namespace }}

2. Cache Status:
{{- if .Values.redis.enabled }}
   Redis is deployed as a subchart ({{ .Release.Name }}-redis-master)
{{- else }}
   Using external Redis at {{ .Values.redisConfig.external.host }}
{{- end }}

3. Search Status:
{{- if .Values.elasticsearch.enabled }}
   Elasticsearch is deployed ({{ .Release.Name }}-elasticsearch)
{{- else }}
   Elasticsearch is disabled
{{- end }}

4. Check deployment status:
   kubectl get pods -n {{ .Release.Namespace }}

5. Access logs (FrankenPHP serves both HTTP and PHP):
   kubectl logs -f deployment/{{ include "bagisto.fullname" . }} -c bagisto -n {{ .Release.Namespace }}

For more information, visit: https://bagisto.com/en/docs/

Happy selling with Bagisto!
